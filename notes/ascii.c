/*
#include <stdio.h>
#include <cs50.h>

int main(void)
{

    string message = get_string("Message: ");

    for (int i = 0; i < message[i] != '\0'; i++)
    {
    printf("%i \n", message[i]);
    }

}



// This was a demonstration using the hash symbol, but I thought it would be really cool to be able to get the ascii code for any character. So I did this.

// turns out he did this exact thing, pretty much. just using char to output the same string instead of the ascii number

*/